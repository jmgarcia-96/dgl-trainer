import { FilterTabsPipe } from './filter-tabs.pipe';

describe('FilterTabsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTabsPipe();
    expect(pipe).toBeTruthy();
  });
});
